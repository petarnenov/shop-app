export const getIsCartOpen = (state) => state.cartDropdown.isCartOpen;

export const getCartItems = (state) => state.cartDropdown.cartItems;

export const getTotal = (state) => state.cartDropdown.total;

export const getItemsCounter = (state) => state.cartDropdown.itemsCounter;
